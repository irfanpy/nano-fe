// Sidebar list of all active chat conversations with unread counts
export default function ConversationList({ conversations, activeId, onSelect }) {
  // TODO: implement conversation rows with avatar, last message preview, timestamp
}
