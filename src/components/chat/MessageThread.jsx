// Scrollable message thread for a single conversation
export default function MessageThread({ messages, currentUserId }) {
  // TODO: implement bubble layout, auto-scroll to bottom, timestamp grouping
}
