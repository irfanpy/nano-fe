// Reusable confirmation dialog (e.g. cancel booking, delete account)
export default function ConfirmDialog({ open, onClose, onConfirm, title, description, danger }) {
  // TODO: implement with Modal, warning icon, confirm/cancel buttons
}
