// User avatar with fallback initials and size variants
export default function Avatar({ src, name, size = 'md', verified }) {
  // TODO: implement with initials fallback and verified checkmark overlay
}
