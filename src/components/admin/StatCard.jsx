// KPI statistic card for admin dashboard (number, label, trend indicator)
export default function StatCard({ label, value, trend, icon, color }) {
  // TODO: implement metric card with colored icon and percentage trend arrow
}
