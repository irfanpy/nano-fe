// Hero section of a helper's public profile: large photo, name, badges, rating, CTA
export default function HelperProfileHeader({ helper }) {
  // TODO: implement profile hero with cover, avatar, verified checkmark, and contact CTA
}
