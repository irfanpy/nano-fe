// Browse-listing card for a single helper (photo, name, category, rating, price)
export default function HelperCard({ helper, onShortlist }) {
  // TODO: implement card with photo, verified badge, category tag, star rating, AED/month price, shortlist heart
}
