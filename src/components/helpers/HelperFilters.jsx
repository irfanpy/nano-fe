// Search filter panel: category, nationality, price range, availability, rating
export default function HelperFilters({ filters, onChange }) {
  // TODO: implement filter sidebar / drawer with react-hook-form
}
